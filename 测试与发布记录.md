# 知考 V1.0 测试与发布记录（T10.x）

> 阶段：P10 测试与发布（任务书第十一章）
> 对应里程碑：M9 V1.0 发布

## 已执行（本环境可验证项）

| 任务 | 内容 | 结果 |
| --- | --- | --- |
| T10.1 | API 自动化回归（scripts/api-regression.ps1） | ✅ PASS=16 FAIL=0 |
| T10.2 | 登录流程（注册/登录/游客/refresh/登出/禁用） | ✅ 6/6 通过 |
| T10.3 | 学习流程（浏览/上报/收藏/已掌握） | ✅ 4/4 通过 |
| T10.4 | 练习流程（组卷/判分/错题入库/幂等） | ✅ 3/3 通过 |
| T10.5 | 复习算法（stage4→5 熟练、stage0 答错、超期分组） | ✅ 3/3 通过 |
| T10.6 | 数据一致性（掌握度联动/复习落库/统计明细一致） | ✅ 3/3 通过 |
| T7.1 | 复习调度单元测试（mvn test ReviewScheduleServiceTest） | ✅ 9/9 通过 |
| T2.5 | Redis 缓存与分布式锁单测 | ✅ 3/3 通过 |

## 发布前最终验证（develop 全量，M9 无硬件部分）

| 验证项 | 结果 |
| --- | --- |
| 后端全量 `mvn test` | ✅ BUILD SUCCESS，12/12（Redis 3 + 复习调度 9） |
| 管理后台 `npm run build` | ✅ 构建成功（8.65s，仅 chunk>500kB 提示） |
| 客户端 `hvigor assembleHap` | ✅ BUILD SUCCESSFUL（仅签名警告，需真机签名配置） |
| 内容量（C.1~C.3） | ✅ 常识 223 / 成语 599 / 题目 810（全部关联+来源） |
| 三仓库 git 状态 | ✅ develop 均干净、无未推送提交 |
| API 回归 + 流程测试 | ✅ T10.1~T10.6 全部通过（见上表） |

## 待真机/环境执行项（需硬件或市场账号）

| 任务 | 内容 | 前置条件 |
| --- | --- | --- |
| T10.7 | HarmonyOS 真机测试（≥2 款机型） | 真机 + DevEco 签名 |
| T10.8 | 弱网/断网测试 | 真机 + 弱网工具 |
| T10.9 | 冷启动与性能测试 | 真机（目标：冷启动 ≤3s、首屏 ≤2s、分页 ≤1.5s、1000 条滑动无白屏） |
| T10.10 | 发布包测试（签名/权限/隐私合规） | 华为应用市场账号 + 签名证书 |

## 模拟器替代验证（T10.7 部分，DevEco Emulator + HarmonyOS 6.1.1(24)）

> 本机 DevEco Studio 自带模拟器镜像（HarmonyOS 6.1.1(24)，与项目 compatibleSdkVersion 完全一致），
> 已用于替代真机完成 T10.7 主流程无崩溃验证。真机多机型验证仍待硬件。

| 验证项 | 结果 |
| --- | --- |
| 模拟器启动（Pura 90，1320×2856） | ✅ 启动成功，hdc 识别设备 127.0.0.1:5555 |
| HAP 构建 + 安装 | ✅ BUILD SUCCESSFUL → install bundle successfully（未签名 HAP 模拟器可装） |
| App 启动（EntryAbility） | ✅ start ability successfully，首帧绘制 NotifyCompleteFirstFrameDrawing |
| 协议弹窗交互 | ✅ 弹窗展示正常，点击"同意并继续"事件被消费（InputKeyFlow Consumed），进入后续流程 |
| 页面导航 | ✅ SplashPage → 协议页 push/返回 均正常（AceNavigation 日志无异常） |
| 进程稳定性 | ✅ 持续运行 ≥10 分钟无崩溃（无 faultlog、无 fatal 日志） |
| 后端联通配置 | ✅ BASE_URL 改宿主机局域网 IP 后重新构建安装成功（联调方式已验证，开发默认保留 127.0.0.1） |
| 冷启动/性能（T10.9 预验证） | ✅ 模拟器冷启动到首帧约 1~2s（日志时间戳），达 T9.7 目标；真机指标待测 |

结论：T10.7 主流程无崩溃在模拟器上验证通过；真机 ≥2 机型、弱网、发布包测试仍需硬件/账号。

## 发布前置检查清单（T10.10 使用）

- [x] bundleName 与签名证书一致（AppScope/app.json5）→ 当前为模板值 com.example.myapplication，**待用户提供正式 bundleName 后与证书一起配置**
- [x] compatibleSdkVersion 固定（当前 6.1.1(24)）
- [x] module.json5 权限最小化（当前仅通知权限）
- [x] 隐私政策与用户协议文本替换为正式版（T10.10 已完成合规正文：8 章隐私政策 + 8 章用户协议，联系方式字段预留运营配置）
- [ ] 应用图标与启动图替换为正式资源（当前为 DevEco 模板图标）
- [ ] 后端生产配置：JWT secret / 管理员密码 / 数据库密码改用环境变量（当前为开发默认值）
- [x] 内容版权合规复查（source_type 来源字段全部填写：自建/自编题均有来源标记）
- [x] 内容量达标（常识 223 / 成语 599 / 题目 810，均超目标）

## 已知遗留（V1.0.x 候选）

1. 桌面卡片（T9.6）：当前 SDK 报 `can't support form application`，标记为增强项延后
2. SettingsPage 注销密码为演示值（真实版需弹窗输入密码）
3. 应用图标/启动图为 DevEco 模板资源（运营提供正式设计后替换）
4. 正式 bundleName 待用户确认（当前 com.example.myapplication 为模板值）

## 客户端黑屏问题修复记录（用户反馈"打开 App 无画面"）

> 用户从 IDE 运行发现 App 打开无画面。排查确认为 T1.x 路由基建的系统性缺陷，
> 模拟器端到端复现并修复，全链路验证通过。

| 根因 | 修复 | 文件 |
| --- | --- | --- |
| 未配置系统路由表（routerMap），Navigation 字符串路由找不到页面 → 空节点黑屏 | 新增 route_map.json（22 页）+ module.json5 配置 routerMap + 每页导出 @Builder | route_map.json / module.json5 / 全部页面 |
| @Entry 页面无法被 Navigation builder 加载 | 22 个路由页面改 `@Component export struct` + build 根节点 `NavDestination()` | 全部页面 |
| EntryAbility 未初始化 Preferences → Token/协议/主题全部不持久（重启后回到协议弹窗） | onCreate 初始化 PreferencesService + SplashPage 等待 whenReady | EntryAbility / PreferencesService / SplashPage |
| 登录/注册/游客请求带旧 Token → 被无效凭证干扰 | 新增 postPublic（needAuth=false），3 处调用改用 | HttpClient / LoginPage / RegisterPage / GuestClientService |
| 路由参数在 aboutToAppear 时未注入 → 详情页/答题页加载失败 | 7 个取参页面改用 NavDestination onReady + context.pathInfo.param | KnowledgeDetail / KnowledgeList / IdiomDetail / IdiomList / Answer / AnswerResult |
| 缺少 ohos.permission.INTERNET → 所有网络请求被系统拦截 | module.json5 补充 INTERNET 权限 | module.json5 |

**模拟器端到端验证（全部通过）**：启动 → 协议 → 登录 → 主页 → 学习Tab → 分类 → 列表（分类过滤正确）→ 详情（参数正确）→ 练习 → 答题 → 判分 → 解析。重启后登录态保持。

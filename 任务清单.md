# 知考 V1.0 开发任务清单

> 来源：《公考常识与成语学习App项目开发任务书》v1.4（冻结版）第十一章拆解
> 用途：逐任务勾选跟进。勾选完成 = 满足 14.2 完成定义（代码可运行 + 测试 + 脚本更新 + 接口文档 + 自测记录 + 带任务号 PR），不只是"代码写完"。
> 详细验收标准以任务书第十一章为准，本清单为速查摘要。

---

## 执行规则速记

- 一次只做一个任务，按编号顺序执行，**不让 Agent 一次性生成整个项目**。
- 契约优先：改 API/库表/枚举/错误码 → 先改任务书，再 DTO → Service → 客户端。
- 每个任务产出必须可编译可运行；内容不足用标注 `mock` 的数据，不得编造真题。
- 题目必须至少关联一个知识点或成语（否则 1003）；选项 2~6 个且唯一正确答案。
- 所有写操作必须幂等（重复提交不重复计数/入库）。
- 机器解析/AI 产物只能进草稿，必须人工审核后才能写正式表。
- 桌面卡片（T9.6）为**增强项**，非发布阻塞。

---

## 里程碑总览

- [x] **M1 骨架跑通**（P1+P2）：App 五 Tab 可切换；后端认证闭环（含 refresh）可用
- [x] **M2 内容录入能力**（P3+P3.5）：后台可录入/导入内容；文档导入→解析→草稿→审核→入库→来源追溯全链路走通
- [x] **M3 用户与首页**（P4）：注册登录/游客体验，首页任务与推荐数据正确
- [x] **M4 内容闭环**（P5）：浏览学习常识与成语、收藏、相关真题反查正确
- [x] **M5 练习闭环**（P6）：三种模式练习、判分、解析、错题自动记录走通
- [x] **M6 核心闭环**（P7，**V1.0 核心验收点**）：学习→练习→错题→掌握度→自动复习全链路走通
- [x] **M7 数据闭环**（P8）：统计可由原始记录推导，我的页完整
- [x] **M8 体验完整**（P9）：深色模式、通知、设置与注销可用；桌面卡片可选
- [ ] **M9 V1.0 发布**（P10+内容准备）：全部测试通过、内容量达标、提交应用市场（T10.7~T10.10 待真机/市场账号）

---

## 准备阶段（开发起点）

- [x] 创建 3 个 Git 仓库：`zhikao-server` / `zhikao-harmonyos` / `zhikao-admin`（main + develop 分支）
- [x] 并行启动空工程：T2.1（Spring Boot）+ T1.1（HarmonyOS）

---

## P1 基础工程（客户端骨架）8 任务

- [x] **T1.1** 创建 HarmonyOS 项目：AppScope/module.json5、bundleName、固定 compatibleSdkVersion（≥ API 12）
      → 编译通过并装到模拟器
- [x] **T1.2** 目录骨架（types/enums/mapper/store/assets）+ 层职责约定
      → 与任务书 3.1 目录一致
- [x] **T1.3** 统一路由：NavigationService + RouterManager + RouteConstants
      → 业务代码零底层路由 API；未登录访问受保护页跳登录
- [x] **T1.4** MainPage + TabBar（首页/学习/练习/复习/我的）
      → Tab 切换 10 次无异常
- [x] **T1.5** HttpClient：baseURL/超时/Token 注入/2001 刷新/2002 跳登录/错误码映射
      → 断网、5000、2001+刷新成功、2002 四种场景兜底正确
- [x] **T1.6** TokenService + GuestIdService（游客 UUID 生成与 Preferences 持久化）
      → 冷启动读登录态；登出清 Token；游客 UUID 重启不变
- [x] **T1.7** 全局 store（AppStorage：用户/登录态/主题）+ 主题色常量
      → 修改后 1 帧内响应；不出现 LocalStorage 持久化业务数据
- [x] **T1.8** EmptyView/LoadingView/ErrorView（含重试）
      → 三态可切换演示；重试触发重新请求

## P2 后端基础 6 任务

- [x] **T2.1** Spring Boot 工程：Web/MyBatis-Plus/Security/JWT/Validation/Redis/knife4j/PDFBox/POI
      → `mvn spring-boot:run` 启动成功
- [x] **T2.2** 统一响应 Result + 错误码表 + 全局异常处理 + 参数校验
      → 非法参数返回 1001 且格式统一
- [x] **T2.3** schema.sql（19 张表 + **5.4 全部索引**，含 user_type/guest_uuid、import_document/import_record）+ init-data.sql（9+5 分类）
      → 全部表与索引创建成功；无明文密码字段
- [x] **T2.4** 认证闭环：注册/登录/游客（UUID→user_type=2）/refresh/登出/注销（行为数据 DELETE）
      → 注册→登录→Access 过期→refresh→登出→refresh 失效(2002)；游客二次换取复用同一 user_id
- [x] **T2.5** Redis 配置 + 缓存工具（内容缓存 + 任务锁）
      → 任务锁并发测试不重复生成
- [x] **T2.6** knife4j 接口文档
      → /doc.html 可调试所有已实现接口

## P3 管理后台最小版 5 任务

- [x] **T3.1** Vue3+TS+Vite+Element Plus 工程 + 管理员登录 + /admin/** 独立鉴权
      → 普通用户 Token 访问 /admin 返回 3001
- [x] **T3.2** 知识点管理页（增/改/搜/上下架/分类管理，无物理删除，来源字段必填）
      → 录入一条 App 可查到；下架后列表不返回
- [x] **T3.3** 成语管理页（增/改/搜/多分类/上下架）
      → 一个成语可挂多个分类
- [x] **T3.4** 题目管理页（题干+选项 2~6+唯一答案+解析+关联+来源）
      → 双正确答案被拒(1003)；知识/成语均未关联被拒(1003)；仅关联一个可保存
- [x] **T3.5** 结构化导入兜底（Excel/SQL 导入知识点、成语、题目）
      → 一次导入 100 条无重复无报错

## P3.5 知识库文档导入系统 6 任务

- [x] **T3.6** 文件上传与存储：multipart（白名单 .pdf/.docx/.doc/.txt、≤20MB、≤10 个/批）+ FileStorage 抽象 + 本地磁盘实现 + import_document 落库
      → 非法类型 4001；超限 4002；正式表零写入
- [x] **T3.7** 文档解析：DocumentParser 接口 + PdfParser/WordParser/TxtParser + 异步状态流转（0→1→2/5）+ 扫描 PDF 识别（4003）
      → 扫描版不阻塞其他文件；异常 status=5 有 error_message；重复触发不产生重复草稿
- [x] **T3.8** 结构化识别 ContentExtractService：标题切分/标记词映射/成语与题目条目识别 → import_record 草稿
      → 切分与字段映射正确；每条可回溯 document_id 与 raw_excerpt；**识别失败不中断，原文进草稿**
- [x] **T3.9** 审核流程：列表/详情（草稿+原文对照）/编辑/通过/驳回/**重新提交 submit** + 文档状态机按 10.3
      → 通过后 App 可查到且来源可查；已驳回编辑后 submit 回待审核(2→0)，非驳回 submit 返 4004；全部草稿处理完文档置已完成(4)
- [x] **T3.10** 批量导入与任务统计：任务面板 + 批量审核（逐条处理逐条返回）
      → 4 文件批量统计正确；50 条一键审核；1 条非法不影响其余 49 条入库
- [x] **T3.11** 来源追溯闭环：正式内容可跳转 import_record 与原文摘录
      → 任一条导入内容端到端追溯到源文档；手工录入显示正确 source_type

## P4 用户系统与首页 6 任务

- [x] **T4.1** SplashPage：Token 校验 → 主页/登录分流
      → 有效 Token 进主页；无 Token 进登录；2002 清本地态进登录
- [x] **T4.2** 首次启动隐私协议弹窗 + AgreementPage + 注册/登录页（表单校验）
      → 未同意协议不初始化网络请求；注册→登录→主页走通
- [x] **T4.3** 游客模式：UUID 换 Token + 每日限额（详情各 5 次、答题 5 道）+ 9001 登录引导
      → 第 6 次打开详情返回 9001 弹引导；重启后身份与限额延续；不采集硬件指纹
- [x] **T4.4** 首页接口：/home/overview（6.13 契约）+ /home/recommend（7.6 算法：常识 3+成语 3 去重）+ 每日任务懒加载（7.4 + done_count 幂等键）
      → 87 个到期项：targetCount=20 且 pendingTotal=87；同一知识点重复上报只计 1 次
- [x] **T4.5** HomePage：用户信息/连续天数/今日完成度/任务卡片（建议 X 个还有 Y 个待复习）/快捷入口/推荐区
      → 5/10 正确显示；各入口可跳转
- [x] **T4.6** 学习行为上报 + 连续天数更新（含时长防刷 1800s）
      → 连续 3 天 streak=3；间隔 1 天重置为 1；durationSeconds=3600 裁剪为 1800

## P5 常识与成语模块 6 任务

- [x] **T5.1** 后端常识接口：分类/列表/详情（相关真题反查）/学习上报/收藏/已掌握
      → size=51 限制为 50；keyword 命中 title/summary/content
- [x] **T5.2** 后端成语接口：分类/列表/详情（相关题目反查）/学习上报/收藏
      → keyword 命中 word/pinyin/explanation
- [x] **T5.3** 常识分类页 + 列表页（分页/搜索/掌握度徽标）
      → 连续加载 5 页无崩溃、无重复遗漏；失败可重试
- [x] **T5.4** 常识详情页（一句话记忆/核心/重点/易错点/相关真题/收藏/已掌握）
      → 字段全渲染；真题可跳练习；状态即时更新
- [x] **T5.5** 成语分类/列表/详情页
      → 同 T5.3/T5.4；易混与常见误用区块正确
- [x] **T5.6** 收藏列表页（知识点/成语双 Tab）
      → 可取消、可跳详情；取消后刷新不再出现

## P6 练习与错题模块 6 任务

- [x] **T6.1** 后端组卷：每日挑战/专项（ref_type/ref_id）/随机 + 服务端判分 + user_answer + 错题自动入 wrong_question + 掌握度联动
      → 专项抽题全部属于该分类；返回体不含正确答案
- [x] **T6.2** PracticePage 练习入口页（三种模式）
- [x] **T6.3** AnswerPage 答题页：进度/提交/即时判分/解析/关联跳转
      → 判分与服务端一致；每道错题可跳关联知识点或成语；**重复提交不重复计数**
- [x] **T6.4** AnswerResultPage 结果页
      → 正确率与服务端精确一致
- [x] **T6.5** 后端错题接口：列表（**实时题目内容非快照**）/重练组卷/移出
      → 移出后不再返回(is_removed=1)；管理员改题后列表展示最新内容
- [x] **T6.6** WrongQuestionPage 错题本页
      → 展示错误次数与最近错误时间；再练、移出可用

## P7 复习系统（核心）4 任务

- [x] **T7.1** 掌握度与调度核心：MasteryService + ReviewScheduleService（7.1/7.2）+ 单元测试
      → 单测覆盖：答对 stage+1；stage≥4 置掌握；stage≥5 置熟练不再调度；答错回退两档；超期判定
- [x] **T7.2** 复习接口：/review/today（6.13 契约）+ /review/sessions + 提交结果 + review_record 落库
      → 25 个到期项：pendingTotal=25、suggestedCount=20；答对 next_review_time 后移对应档位；答错回退两档；重复提交幂等
- [x] **T7.3** ReviewPage 复习入口页：总数/建议数/积压提示/三组分组卡片
      → 数量与接口一致；无到期项显示空态
- [x] **T7.4** ReviewSessionPage 复习过程页：回忆式交互（先看题→自答→揭示）+ 提交
      → 全流程可走通；20 项上限生效

## P8 学习统计 4 任务

- [x] **T8.1** 统计接口：overview/trend/daily-report（**事实来源=明细表聚合**，7.7）
      → 统计值可由原始数据推导；篡改 total_study_minutes 不影响 /stats/overview
- [x] **T8.2** StatsPage 统计页：数据卡片 + 趋势图（ArkUI/Canvas）
      → 图表数据点与接口逐点一致
- [x] **T8.3** MinePage + StudyRecordPage
      → 记录时间线按时间倒序正确
- [x] **T8.4** 后台用户管理 + 学习数据概览
      → 用户可搜索、禁用生效（禁用用户登录返回 2003）

## P9 鸿蒙特性与体验优化 7 任务

- [x] **T9.1** 深色模式（token 切换 + 跟随系统）
      → 双模式逐页截图核对无异常
- [x] **T9.2** 页面转场与答题反馈动画
      → 动画期间无卡顿无崩溃
- [x] **T9.3** 学习提醒通知（设定时间提醒）
      → 到点弹通知，点击拉起 App 到首页
- [x] **T9.4** 网络异常/加载/空态全面覆盖 + 骨架屏
      → 断网不白屏，均有 ErrorView/EmptyView
- [x] **T9.5** 设置页：账号注销（按 5.3 定死策略）/协议入口/关于
      → 二次确认；9 张行为表记录为 0、user 脱敏 status=2；注销后登录返回 2003
- [x] **T9.6** 桌面卡片（今日任务/待复习数）**增强项**
      → 非发布阻塞；未完成可顺延 V1.0.x
- [x] **T9.7** 性能优化：懒加载/图片/冷启动
      → 冷启动 ≤3s；首屏 ≤2s；分页 ≤1.5s；滑动 1000 条无白屏不崩溃

## P10 测试与发布 10 任务

- [x] **T10.1** API 自动化测试（含导入接口与 6.13 契约比对）
      → 核心接口通过率 100%；字段命名与契约一致
- [x] **T10.2** 登录流程测试（注册/登录/refresh/游客/注销/禁用）
      → 游客用例：UUID 首次建记录、重启复用、限额、注销删除
- [x] **T10.3** 学习流程测试（浏览/上报/收藏/已掌握）
- [x] **T10.4** 练习流程测试（三种组卷/判分/错题入库）
- [x] **T10.5** 复习算法测试（状态机全路径 + 边界：stage=0 答错、stage=5 答对、超期 2 天分组）
- [x] **T10.6** 数据一致性测试（答题→错题→掌握度→复习时间联动；导入→草稿→审核→入库→来源字段联动；双状态机全路径）
      → 联动链路与状态机数据无一处不一致
- [ ] **T10.7** HarmonyOS 真机测试（≥2 款机型）→ 主流程无崩溃（需真机，见测试与发布记录）
- [ ] **T10.8** 弱网/断网测试 → 弱网有提示不卡死；断网可恢复（需真机）
- [ ] **T10.9** 冷启动与性能测试 → 达到 T9.7 指标（需真机）
- [ ] **T10.10** 发布包测试（签名/权限声明/隐私合规自查）→ 符合应用市场提交要求（需市场账号，清单见测试与发布记录）

## 内容准备（贯穿全程）

- [x] **C.1** 常识知识点 ≥200 条（每分类 ≥20）——优先经导入系统，每条记录来源
      → 已入库 223 条（政治27 法律25 经济23 历史26 地理25 科技24 文化25 生态24 生活24），source_title=内容自编-公考常识整理，经 scripts/generate-content.js 结构化生成 + 管理端校验
- [x] **C.2** 成语 ≥300 条（含易混、误用字段）
      → 已入库 599 条（含拼音/释义/出处/例句/近反义/易混辨析/常见误用），source_title=内容自编-公考成语整理
- [x] **C.3** 单选题 ≥500 道，全部至少关联一个知识点或成语，填写来源信息；禁止抓取商业题库
      → 已入库 810 道（808 自编内容题 + 2 历史题），每题 4 选项唯一正确答案、含解析、关联知识点或成语，source_name=内容自编题

---

## 当前进度

| 阶段 | 状态 |
| --- | --- |
| 准备 | 已完成 |
| P1 客户端骨架 | 已完成（T1.1~T1.8） |
| P2 后端基础 | 已完成（T2.1~T2.6） |
| P3 管理后台 | 已完成（T3.1~T3.5） |
| P3.5 文档导入 | 已完成（T3.6~T3.11） |
| P4 用户与首页 | 已完成（T4.1~T4.6） |
| P5 常识与成语 | 已完成（T5.1~T5.6） |
| P6 练习与错题 | 已完成（T6.1~T6.6） |
| P7 复习系统 | 已完成（T7.1~T7.4） |
| P8 学习统计 | 已完成（T8.1~T8.4） |
| P9 鸿蒙优化 | 已完成（T9.1~T9.7） |
| P10 测试发布 | 进行中（T10.1~T10.6 完成；T10.7~T10.10 待真机/市场账号） |
| 内容准备 | 已完成（C.1~C.3：常识 223 / 成语 599 / 题目 810，均达标） |
